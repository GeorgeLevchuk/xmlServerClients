# Java Project from XSD Schema

## Генерация Java классов из XSD схемы

Для автоматической генерации Java классов на основе XSD схемы выполните следующую команду:

```bash
mvn clean compile
```

После запустите сервер XmlServer.java и клиентов XmlClient.java в IDEA